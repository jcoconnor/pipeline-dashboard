package constants

// A holding place for a few constants we are using through the code.
// the Single Source of Truth bit

const Trains_filename = "trains.csv"
const Results_filename = "result.csv"

const SCRAPE_INTERVAL = 3600
